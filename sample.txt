# トップレベル(タイトル等) [0, ,0]
どんなノードも内容を持てます。

## 2レベル-1 [0, ,0]
//aaaaaaaaaaa
ノードタイトル行と
「//」で始まる行は文字数カウントされません
oコマンド(ファイル出力)時にも出力の対象になりません。

### 3レベル-1 [0, ,0]
行頭の「#」の数でノードの深さを示します。
また「#」(複数個)のあとに半角スペースを挟んで続く文字列がノードタイトルとなります。
ノード内はプレーンなテキストとして扱われ、改行コード(lf)が出現するまでは改行もされません。
折り返し表示を行う場合は、指定のテキストエディタやビューワ側で折り返し設定してください。

[「#」×複数][半角スペース][ノードタイトル][半角スペース][メタデータ]

で定義されます。

##2レベル [,,]
↑スペースが挟まっていないのでノードとして認識されない

### 3レベル-2 [0, ,0]
2レベル-1がそうであるように、内容を持たないノードも作れます。

## 完了ノード [1, ,0]
ノードタイトルの後ろに半角スペースを挟んで[完了フラグ,シンボル,非表示フラグ]の形式でメタデータを設定します。
完了フラグを1にすると、そのノードが執筆完了しているフラグがオンになります。
このフラグは、動作指定「t」では表示されませんが、詳細表示である「ta」を使うと表示できます。
完了フラグのオン/オフは、動作指定cで切り替えます。

## 未完了ノード-2 [0, ,0]
また、完了フラグに1が設定されていないノードでは、未設定を0と読み替えているだけで、
本来は0が未完了です。
未完了を明示的に設定するには、メタデータの1番目に0を設定するか、
完了状態から、動作指定cで完了状態を解除します(1が削除されるのではなく0が設定されます)。

######### 深さを飛躍したノード [0, ,0]
このアウトライナーでは、深さが1ずつ深くなっていくことを強制しません。
(深さ1のノードの次には1か2しか設定できない、という縛りがありません)

# トップノード2 [,,]
トップノードは複数あっても問題ありません。

## ノードの属性 [0,🔞,]
ノードには1文字ぶんのラベル表示域を用意しています。
主に絵文字などでラベリングすることを想定しています。
ラベルシンボルは、メタデータの2番目に設定してください。
あるいは動作指定「s」で付与できます(sにて何も指定しない場合はシンボルが削除されます)。
２文字以上の文字を設定しても１文字だけが適用されます。
